function sum(a, b) {
    return a + b;
}

module.exports = {
    sum,
    name: "hello world",
    age: 23
}
// In the above line we are exporting
// an object which has the sum function as a property.
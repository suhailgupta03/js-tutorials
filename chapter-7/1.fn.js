/**
 * Following is a function definition:
 *  - that adds two numbers 10 and 20
 *  - and prints the result
 */
function sum() {
    var x = 10;
    var y = 20;
    var z = x + y;
    console.log(z);
}

sum(); // to call the function,we use the function name followed by parenthesis

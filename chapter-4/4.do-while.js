var x = 0;
do { // body of the loop that will be executed at least once
    // code block to be executed
    console.log(x);
    x++;
}while(x <= 1); // condition

/**
 * do-while loop is used when you want to execute the code block 
 * at least once,
 */